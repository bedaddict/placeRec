{
  "buildCommand": "cd place-rec-frontend && npm install && npm run build",
  "outputDirectory": "place-rec-frontend/dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/main.py"
    }
  ]
}